# IT Security Python Exercises Dependencies

# Core cryptography library for AES implementations
pycryptodome>=3.19.0

# Standard library modules used (no installation needed):
# - math (for gcd, etc.)
# - time (for performance measurements)